<?php
use Application\Controller\CoreController;

# Load Widgets for User
$aDashboardWidgets = CoreController::$oSession->oUser->getMyWidgets();
?>
<div class="row">
<?php
if(count($aDashboardWidgets) > 0) {
    foreach($aDashboardWidgets as $oWidget) {
        echo $this->partial('widget/'.$oWidget->widget_name,['oUser'=>CoreController::$oSession->oUser]);
    }
}
?>
</div>

